{
  "name": "nexsql",
  "version": "0.1.0",
  "private": true,
  "description": "NexSQL - AI-Powered Database Client",
  "scripts": {
    "dev": "pnpm --filter nexsql-desktop dev",
    "build": "pnpm --filter nexsql-desktop build",
    "build:win": "pnpm --filter nexsql-desktop build:win",
    "build:mac": "pnpm --filter nexsql-desktop build:mac",
    "build:linux": "pnpm --filter nexsql-desktop build:linux",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=6.0.0"
  }
}
